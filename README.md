# nikud-benchmark

## Scripts

```
uv run src/stats.py           # dataset statistics (word counts, diacritics by type)
uv run src/check_integrity.py # verify nikud forms match plain forms (ktiv male/haser)
```
